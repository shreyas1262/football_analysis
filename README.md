# football_analysis

A learn-by-building football analytics project covering the full modern data and AI engineering stack. Ingests match data from football-data.org via Apache Airflow, transforms it through a medallion architecture using dbt and PostgreSQL, then layers an AI tier on top — including a Claude-powered Q&A agent with tool calling, RAG over match reports via pgvector, natural language to SQL, auto-generated match narratives, anomaly detection, and an evaluation framework. Built without frameworks like LangChain — every pattern implemented from first principles using the Anthropic SDK directly.

---

## Stack

| Layer | Technology |
|---|---|
| Ingestion | Apache Airflow, football-data.org API |
| Storage | PostgreSQL 15 (pgvector/pgvector:pg15) |
| Transformation | dbt (staging → intermediate → marts) |
| AI / LLM | Anthropic Claude (claude-sonnet-4-6) |
| Embeddings | Voyage AI (voyage-3, 1024 dims) |
| Vector search | pgvector |
| MCP server | mcp Python SDK |
| Testing | pytest |

---

## Project structure

```
football_analysis/
├── airflow/
│   ├── dags/football_ingestion.py      # Airflow DAG — fetches & loads match data
│   └── plugins/football_api_client.py  # football-data.org API wrapper
├── dbt/
│   └── models/
│       ├── staging/                    # Raw → typed source models
│       ├── intermediate/               # int_team_form rolling window
│       └── marts/                      # mart_league_table, mart_match_results,
│                                       # mart_bottler_index, match_reports,
│                                       # report_embeddings
├── mcp/
│   └── football_mcp_server.py          # MCP server exposing 6 DB tools
├── agent/
│   ├── config.py → (root config.py)    # Shared DB_CONFIG
│   ├── football_agent.py               # Agentic loop — 8 tools, Claude tool use
│   ├── nl_to_sql.py                    # NL → SQL pipeline with validation & retry
│   ├── rag_retrieval.py                # Semantic search over match narratives
│   ├── generate_and_store_narratives.py# Generates & embeds match reports
│   ├── match_narratives.py             # Single-match narrative generation
│   ├── anomaly_detection.py            # Structured anomaly detection via tool use
│   └── evaluation.py                   # Three-layer evaluation framework
├── tests/
│   ├── conftest.py                     # Shared fixtures (db_conn, sample_match)
│   ├── test_nl_to_sql.py               # validate_sql unit tests
│   ├── test_rag_retrieval.py           # chunk_text + embedding dimension tests
│   ├── test_mcp_server.py              # Tool schema + DB integrity tests
│   └── test_evaluation.py             # deterministic_checks + golden dataset tests
├── config.py                           # Shared DB_CONFIG (env-driven)
├── docker-compose.yml                  # PostgreSQL + pgvector
└── requirements.txt
```

---

## Data

Covers the **2024/25 season** for four competitions:

| Code | Competition |
|---|---|
| PL | Premier League |
| PD | La Liga |
| BL1 | Bundesliga |
| SA | Serie A |

**Not available:** Champions League, individual player stats (xG, assists, minutes), transfer or injury data.

---

## AI features

### Football agent (`agent/football_agent.py`)
Claude agent with an 8-tool agentic loop. Answers natural language questions about match results, standings, form, and narratives. Tools:

- `get_league_table` — current standings with performance metrics
- `get_bottler_index` — teams ranked by half-time lead drop rate
- `get_team_form` — last N matches with rolling form points
- `get_head_to_head` — historical results between two teams
- `get_high_scoring_matches` — matches filtered by total goals
- `get_season_summary` — competition-level aggregate stats
- `search_match_reports` — semantic RAG search over narratives
- `nl_to_sql` — ad-hoc questions via generated SQL

### NL-to-SQL (`agent/nl_to_sql.py`)
Translates natural language to validated, executable PostgreSQL. Pipeline: generate → validate (regex word-boundary checks + sqlparse type check) → execute → interpret. Retries up to 2× on failure with error context fed back to the model.

### RAG pipeline (`agent/rag_retrieval.py`, `agent/generate_and_store_narratives.py`)
Match narratives generated by Claude (700 tokens, temperature 0.8), chunked at sentence boundaries, embedded with Voyage AI voyage-3 (1024 dims), and stored in pgvector. Cosine similarity search with a 0.4 threshold.

### Anomaly detection (`agent/anomaly_detection.py`)
Structured anomaly detection using Claude tool use with forced `tool_choice`. Returns `is_anomalous`, `severity` (low/medium/high/none), `anomaly_type`, and `explanation` for each match.

### Evaluation framework (`agent/evaluation.py`)
Three-layer evaluation:
1. **Deterministic checks** — hard rules (SQL validity, required keys, forbidden phrases, response length)
2. **LLM-as-judge** — separate Claude call scoring grounding, responsiveness, and accuracy (1–5 each)
3. **Golden dataset** — 25 curated test cases covering factual lookups, cross-tool reasoning, regression tests, and graceful refusals. Pass threshold: 85%

### MCP server (`mcp/football_mcp_server.py`)
Exposes the six DB tools as an MCP server so any MCP-compatible client (Claude desktop, IDEs) can query the football database directly.

---

## Setup

### Prerequisites
- Docker
- Python 3.11+
- Anthropic API key
- Voyage AI API key

### Start the database

```bash
docker-compose up -d
```

### Install dependencies

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### Environment variables

```bash
cp .env.example .env
# Fill in:
# ANTHROPIC_API_KEY=...
# VOYAGE_API_KEY=...
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=football_db
# DB_USER=football
# DB_PASSWORD=football
```

### Run dbt transformations

```bash
cd dbt
dbt run
```

### Generate match narratives and embeddings

```bash
python agent/generate_and_store_narratives.py
```

---

## Running the agent

```bash
python agent/football_agent.py
```

Example questions:
- *"Who is top of the Premier League?"*
- *"Which Bundesliga team has the worst bottler rate?"*
- *"Compare Liverpool and Arsenal this season"*
- *"What were the highest scoring matches in the Bundesliga?"*

## Running evaluation

```bash
python agent/evaluation.py
```

---

## Running tests

```bash
pytest tests/ -v
```

Unit tests run without a database connection. Integration tests (marked with `db_conn` fixture) require the Docker database to be running.
